Incomplete. In-progress. Does not work.

To use, run:


`pip3 install -r requirements.txt` and `pip3 install -e .` Then run:

`python3 -m lfe.examples.working` or `python3 -m lfe.examples.in_progress`

